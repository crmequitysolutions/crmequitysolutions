<p id="notice"><%= notice %></p>

<p>
  <strong>Property:</strong>
  <%= link_to Address.where(["address_id = ?", Property.where(["property_id = ?", @prop_doc.property_id]).first.address_id]).first.line_1, Property.where(["property_id = ?", @prop_doc.property_id]).first %>
</p>

<p>
  <strong>Content:</strong>
  <%= @prop_doc.content %>
</p>

<p>
  <strong>Mime type:</strong>
  <%= @prop_doc.mime_type %>
</p>

<p>
  <strong>File name:</strong>
  <%= @prop_doc.file_name %>
</p>

<p>
  <strong>File type:</strong>
  <%= @prop_doc.file_type %>
</p>

<p>
  <strong>Upload date:</strong>
  <%= @prop_doc.upload_date %>
</p>

<%= link_to 'Edit', edit_prop_doc_path(@prop_doc) %> |
<%= link_to 'Back', prop_docs_path %>
