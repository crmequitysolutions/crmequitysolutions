<p id="notice"><%= notice %></p>

<h1>Investor Prefs</h1>

<table>
  <thead>
    <tr>
      <th><%= link_to "Contact", update_investor_pref_order_path(:contact_id) %></th>
      <th><%= link_to "Zip Code", update_investor_pref_order_path(:zip_code) %></th>
      <th><%= link_to "Community", update_investor_pref_order_path(:community_id) %></th>
      <th><%= link_to "Property Type", update_investor_pref_order_path(:property_type) %></th>
      <th><%= link_to "Bedrooms", update_investor_pref_order_path(:bd_rms) %></th>
      <th><%= link_to "Bathrooms", update_investor_pref_order_path(:ba_rms) %></th>
      <th><%= link_to "Max", update_investor_pref_order_path(:max) %></th>
      <th><%= link_to "Min", update_investor_pref_order_path(:min) %></th>
      <th><%= link_to "Rehab Needed", update_investor_pref_order_path(:rehab_ind) %></th>
      <th><%= link_to "On Main St", update_investor_pref_order_path(:main_st_ind) %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @investor_prefs.each do |investor_pref| %>
      <tr>
        <td><%= link_to Contact.where(["contact_id = ?", investor_pref.contact_id]).first.last_name, Contact.where(["contact_id = ?", investor_pref.contact_id]).first %></td>
        <td><%= investor_pref.zip_code %></td>
        <td><%= link_to Community.where(["community_id = ?", investor_pref.community_id]).first.community_name, Community.where(["community_id = ?", investor_pref.community_id]).first %></td>
        <td><%= investor_pref.property_type %></td>
        <td><%= investor_pref.bd_rms %></td>
        <td><%= investor_pref.ba_rms %></td>
        <td><%= investor_pref.max %></td>
        <td><%= investor_pref.min %></td>
        <td><%= investor_pref.rehab_ind %></td>
        <td><%= investor_pref.main_st_ind %></td>
        <td><%= link_to 'Show', investor_pref %></td>
        <td><%= link_to 'Edit', edit_investor_pref_path(investor_pref) %></td>
        <td><%= link_to 'Destroy', investor_pref, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Investor Pref', new_investor_pref_path %>
